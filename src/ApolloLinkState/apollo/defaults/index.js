// This is Apollos way of setting the default state

export default {
    apolloClientDemo: {
        // This is the default name of the identifier our cache uses
        __typename: 'ApolloClientDemo',
        // the specific item of data that our header will use to display our current page name (with the default state of it set to 'Apollo Demo')
        currentPageName: 'Apollo Demo'
    }
};